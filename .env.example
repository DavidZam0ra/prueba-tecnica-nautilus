# Puerto del servidor
PORT=3000

# URI de conexión a MongoDB
MONGODB_URI=mongodb://localhost:27017/nautilus-tasks

# Clave secreta para firmar los JWT
JWT_SECRET=change_this_secret_in_production

# Tiempo de expiración del JWT (ej: 1h, 7d)
JWT_EXPIRES_IN=1h
